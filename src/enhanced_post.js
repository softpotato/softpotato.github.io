

/**
 * This render function adds optional navigation left side bars,
 * pages to the content, and introduces new render object types
 * to display progress bars, dates, and titles in a natural way
 * similar to Google's tutorials for kotlin. 
 * 
 * Yes, the UI style is pretty much ripped from Google. It's a 
 * gosh darn good style for tutorials. 
 * 
 * @param {*} props 
 */
export default function EnhancedPost(props) {

}